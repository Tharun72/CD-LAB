/* Sample C program to test the lexical analyzer */
int main() {
    int a, b, sum;
    float pi = 3.14;
    a = 10;
    b = 20;
    sum = a + b;
    // compute average
    if (sum > 25) {
        printf("Sum is large");
    } else {
        printf("Sum is small");
    }
    return 0;
}
