/*
 * Lexical analyzer for the three-address-code generator (Experiment 7).
 */

%{
#include "tac.tab.h"
#include <string.h>
#include <stdlib.h>
%}

%%

[a-zA-Z_][a-zA-Z0-9_]*  { yylval.str = strdup(yytext); return ID; }
[0-9]+                   { yylval.str = strdup(yytext); return NUMBER; }
[-+*/();=]                { return yytext[0]; }
[ \t\n]                   ;
.                          ;

%%

int yywrap() { return 1; }
