===== 8086 Assembly Code Generation from Three Address Code =====

.MODEL SMALL
.DATA
    ; declare variables as needed (WORD, 2 bytes each)
.CODE
MAIN PROC
    MOV AX, @DATA
    MOV DS, AX

; TAC: t1 = b + c;
    MOV AX, b
    MOV BX, c
    ADD AX, BX
    MOV t1, AX

; TAC: t2 = t1 * d;
    MOV AX, t1
    MOV BX, d
    MUL BX
    MOV t2, AX

; TAC: a = t2;
    MOV AX, t2
    MOV a, AX

; TAC: t3 = x - y;
    MOV AX, x
    MOV BX, y
    SUB AX, BX
    MOV t3, AX

; TAC: t4 = t3 / z;
    MOV AX, t3
    MOV BX, z
    XOR DX, DX
    DIV BX
    MOV t4, AX

; TAC: result = t4;
    MOV AX, t4
    MOV result, AX

    MOV AH, 4CH
    INT 21H
MAIN ENDP
END MAIN
